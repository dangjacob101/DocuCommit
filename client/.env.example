# URL the Vite dev server proxies /api requests to.
# Default: http://localhost:5000
# macOS users: if you set PORT=5001 in Frank/.env to dodge AirPlay,
# also set this to http://localhost:5001. See SETUP.md.
# VITE_API_TARGET=http://localhost:5001
